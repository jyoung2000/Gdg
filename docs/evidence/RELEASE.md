# Release verification

Run 2026-09-09T19:35:11.820Z. Generated by `node scripts/verify-release.mjs`; every row below
comes from a test that ran in this pass, not from a claim typed by hand.

## Checks

| Check | Result | Duration |
| --- | --- | --- |
| Typecheck | pass | 14.2s |
| Build | pass | 6.6s |
| One version everywhere | pass | 0.1s |
| No known vulnerabilities in the shipped tree | pass | 0.7s |
| Offline artefacts are current and self-contained | pass | 0.1s |
| Docker image builds | skipped — no Docker daemon in this environment | — |
| Live provider checks | skipped — not requested (set MERIDIAN_LIVE_TESTS=1 with credentials configured) | — |
| Scorecard matches the shipped report | pass | 0.1s |
| Unit tests | pass (408) | 17.8s |
| Routing tests | pass (103) | 7.6s |
| Contract and invariants tests | pass (24) | 1.2s |
| Gateway integration tests | pass (27) | 2.2s |
| End to end tests | pass (129) | 31.5s |
| Web client tests | pass (30) | 133.5s |
| Chaos tests | pass (19) | 21.5s |

## Gates

| Gate | Status | Backed by |
| --- | --- | --- |
| Deployment: persistence and container configuration | BLOCKED_EXTERNAL | could not run here: `docker compose up -d` and `docker build` need base images from a registry, which this environment's egress policy refuses. See docs/evidence/DOCKER.md. |
| Core routing | VERIFIED | 4 passing test(s) |
| Anthropic-compatible surface, end to end | VERIFIED | 4 passing test(s) |
| Autonomous coding pipeline | VERIFIED | 5 passing test(s) |
| Fallback and resilience | VERIFIED | 3 passing test(s) |
| Sandbox isolation | BLOCKED_EXTERNAL | could not run here: Container isolation needs a Docker daemon and a sandbox image; this environment has neither. The process-sandbox fallback and its degraded-mode warning ARE verified here. |
| Multimodal surfaces | PARTIAL | 2 passing test(s), covering part of this gate — see below |
| Web client | VERIFIED | 5 passing test(s) |
| Security | VERIFIED | 8 passing test(s) |

### Not covered here (BLOCKED_EXTERNAL)

These are outside the product and are not counted as passing.

- **Deployment: persistence and container configuration** — `docker compose up -d` and `docker build` need base images from a registry, which this environment's egress policy refuses. See docs/evidence/DOCKER.md.
- **Sandbox isolation** — Container isolation needs a Docker daemon and a sandbox image; this environment has neither. The process-sandbox fallback and its degraded-mode warning ARE verified here.
- **Multimodal surfaces** — Image, video, speech and transcription need a credentialed provider; none is configured here.

Every gate that could run here passed. 2 could not run at all — each is listed above with the external dependency it needs, and none of them is counted as passing.
